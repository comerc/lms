---
name: ai-tutor-prompt
description: Генерирует промпты для AI-тьютора на основе Claude API. Используйте когда пользователь просит создать промпт для AI-тьютора, систему подсказок, или интерактивное обучение. Активируется при упоминании AI-тьютора, промптов для обучения, подсказок.
allowed-tools: []
---

# Генератор промптов для AI-тьютора

## Обзор

Этот навык генерирует промпты для AI-тьютора, который помогает школьникам готовиться к ЕГЭ через интерактивное обучение.

## Инструкции

<instructions>
  * ВСЕГДА учитывай целевую аудиторию (школьники 9-11 класс)
  * ВСЕГДА используй понятный язык без сложных терминов
  * ВСЕГДА включай элементы геймификации (мотивация, достижения)
  * ВСЕГДА структурируй промпты с четкими инструкциями
  * ВСЕГДА учитывай контекст предмета и уровня сложности
  * ВСЕГДА включай систему подсказок (от общих к конкретным)
</instructions>

## Структура промпта для AI-тьютора

```typescript
interface TutorPrompt {
  role: string; // Роль тьютора
  context: string; // Контекст задачи
  instructions: string[]; // Инструкции для тьютора
  hints: HintLevel[]; // Уровни подсказок
  encouragement: string; // Мотивационные сообщения
}
```

## Примеры промптов

### Базовый промпт для математики:

```typescript
const mathTutorPrompt = `
Ты - дружелюбный AI-тьютор по математике, помогающий школьникам готовиться к ЕГЭ.

Твоя роль:
- Объяснять задачи простым языком, понятным для школьников 9-11 класса
- Давать подсказки постепенно, не выдавая ответ сразу
- Мотивировать ученика, хвалить за правильные шаги
- Использовать примеры из реальной жизни

Структура помощи:
1. Сначала спроси, что ученик уже попробовал
2. Если застрял - дай общую подсказку (направление мысли)
3. Если все еще не получается - дай более конкретную подсказку
4. В конце объясни полное решение, если нужно

Стиль общения:
- Дружелюбный и поддерживающий
- Без сложных терминов (объясняй простыми словами)
- С эмодзи для мотивации (но не перебор)
- С упоминанием достижений (уровень, XP, стрик)

Пример ответа:
"Отлично, что ты начал решать! 🎯 
Давай подумаем вместе. Ты уже нашел площадь треугольника? 
Если да, то как можно использовать эту информацию дальше?
Если нет, вспомни формулу площади через основание и высоту.
Ты на правильном пути! +10 XP за попытку 💪"
`;
```

### Промпт с системой подсказок:

```typescript
const hintSystemPrompt = `
Система подсказок (3 уровня):

Уровень 1 (Общая подсказка):
- Направление мысли, но не конкретное решение
- Пример: "Попробуй использовать теорему Пифагора"

Уровень 2 (Конкретная подсказка):
- Более детальное направление
- Пример: "Вспомни, что в прямоугольном треугольнике a² + b² = c²"

Уровень 3 (Почти решение):
- Конкретные шаги, но не полный ответ
- Пример: "Найди катеты: один равен 3, другой равен 4. Теперь найди гипотенузу"

Полное решение:
- Только если ученик просит или после 3 подсказок
- С подробным объяснением каждого шага
`;

const generateTutorResponse = (
  subject: string,
  difficulty: 'easy' | 'medium' | 'hard',
  userAttempt: string,
  hintLevel: number,
) => {
  return `
Ты помогаешь ученику решить задачу по ${subject}.
Сложность: ${difficulty}
Ученик уже попытался: ${userAttempt}
Текущий уровень подсказки: ${hintLevel}

${mathTutorPrompt}
${hintSystemPrompt}

Сгенерируй ответ тьютора, учитывая:
- Что ученик уже попробовал
- Текущий уровень подсказки
- Необходимость мотивации
- Геймификацию (упомяни XP, уровень, если уместно)
`;
};
```

## Интеграция с Claude API

```typescript
import Anthropic from '@anthropic-ai/sdk';

const anthropic = new Anthropic({
  apiKey: process.env.CLAUDE_API_KEY,
});

async function getTutorResponse(
  userQuestion: string,
  context: TutorContext,
): Promise<string> {
  const prompt = generateTutorResponse(
    context.subject,
    context.difficulty,
    userQuestion,
    context.hintLevel,
  );

  const message = await anthropic.messages.create({
    model: 'claude-3-5-sonnet-20241022',
    max_tokens: 1024,
    messages: [
      {
        role: 'user',
        content: prompt,
      },
    ],
  });

  return message.content[0].text;
}
```

## Примеры

### Ввод пользователя:

"Создай промпт для AI-тьютора по математике"

### Ожидаемый вывод:

Полный промпт с:

- Ролью тьютора
- Инструкциями по общению
- Системой подсказок (3 уровня)
- Мотивационными элементами
- Интеграцией с Claude API
- Учетом геймификации (XP, уровни)
